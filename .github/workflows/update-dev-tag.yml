name: Update dev tag

on:
  push:
    branches: [dev]

permissions:
  contents: write

jobs:
  tag:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Move dev tag to HEAD
        run: |
          git tag -f dev
          git push origin -f dev
